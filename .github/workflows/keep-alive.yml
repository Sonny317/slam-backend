name: Keep Render Server Awake

on:
  schedule:
    - cron: '*/14 * * * *'  # 매 14분마다 실행
  workflow_dispatch:         # 수동 실행도 가능하게

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Curl ping to Render backend
        run: curl -s https://slam-backend.onrender.com/health