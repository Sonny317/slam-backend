# ===============================================
#  ?? ?? ?? ?? ?? (Default Profile)
# ===============================================

# --- ?? MySQL ?????? ?? ---
spring.datasource.url=jdbc:mysql://localhost:3306/slam_db?serverTimezone=Asia/Seoul
spring.datasource.username=root
spring.datasource.password=4531
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=validate
spring.sql.init.mode=never

# --- 강력한 스키마 보호 설정 ---
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.hibernate.ddl-auto=validate

# --- JWT, ??? ? ?? ???? ?? ---
# (? ??? Render? ?? ??? ??? ?????)
jwt.secret=your-local-jwt-secret-key-for-testing-12345
spring.mail.username=slam.taipei@gmail.com
spring.mail.password=goko fnum sbuf gjck

# --- ?? ??? ?? ?? ---
file.upload-dir=C:/slam-uploads

# --- ?? ?? ---
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.web.resources.static-locations=classpath:/static/
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- Google OAuth 2.0 설정 ---
# Render 환경변수에서 가져오기
google.oauth.client-id=${GOOGLE_CLIENT_ID:YOUR_GOOGLE_CLIENT_ID}
google.oauth.client-secret=${GOOGLE_CLIENT_SECRET:YOUR_GOOGLE_CLIENT_SECRET}
# 프로덕션 환경용 리디렉션 URI
google.oauth.redirect-uri=${GOOGLE_REDIRECT_URI:https://slam-taipei.vercel.app/api/auth/google/callback}