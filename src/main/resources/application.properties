# ===============================================
#  ?? ?? ?? ?? ?? (Default Profile)
# ===============================================

# --- ?? MySQL ?????? ?? ---
spring.datasource.url=jdbc:mysql://localhost:3306/slam_db?serverTimezone=Asia/Seoul
spring.datasource.username=root
spring.datasource.password=4531
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=validate
spring.sql.init.mode=never

# --- 강력한 스키마 보호 설정 ---
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.hibernate.ddl-auto=validate

# --- JWT, ??? ? ?? ???? ?? ---
# (? ??? Render? ?? ??? ??? ?????)
jwt.secret=your-local-jwt-secret-key-for-testing-12345
spring.mail.username=slam.taipei@gmail.com
spring.mail.password=goko fnum sbuf gjck

# --- ?? ??? ?? ?? ---
file.upload-dir=C:/slam-uploads

# --- ?? ?? ---
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.web.resources.static-locations=classpath:/static/
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- Google OAuth 2.0 설정 ---
# Google Cloud Console에서 생성한 클라이언트 ID와 시크릿을 입력하세요
google.oauth.client-id=YOUR_GOOGLE_CLIENT_ID
google.oauth.client-secret=YOUR_GOOGLE_CLIENT_SECRET
# 프로덕션 환경용 리디렉션 URI
google.oauth.redirect-uri=https://slam-taipei.vercel.app/api/auth/google/callback